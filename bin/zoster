#!/usr/bin/env node
var exec = require("../exec");
var args = process.argv.splice(2);
if(args.legth && args.indexOf("help")!=-1) console.log(

var neutron = args.length && args[0] == "--neutron" ? true : false;
exec(neutron);
